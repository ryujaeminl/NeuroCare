# Android install files

`neurocare-patient-v2.20.apk` is the patient app (`com.neurocare.app.patient`).

It is debug-signed so it can be installed directly for testing. Android may require enabling installation from the source used to download the file. The guardian APK is published separately in the Neurocare_care repository.
